{% extends 'admin.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<div class="row">
    <div class="col-lg-3 col-md-6 col-sm-12 col-12">
        <div class="info-box">
            <div class="post">
                <h2>{{ headline }}</h2>
                <br>
                <br>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>

{% endblock %}