{% extends 'admin.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-4 col-12">
        <div class="post">
            <h2>{{ headline }}</h2>
            <br>
            <br>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>

{% endblock %}