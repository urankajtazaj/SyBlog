			<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
				<div class="container">
					<a class="navbar-brand" href="{{ path('post_list') }}"><b>{{ base.pageName }}</b></a>
					<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
					 aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>

					<div class="collapse navbar-collapse" id="navbarSupportedContent">
						<ul class="navbar-nav mr-auto">
						</ul>
						<ul class="navbar-nav">

							{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_WRITER') %}
							<li class="nav-item">
								<div class="dropdown">
									<button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true"
									 aria-expanded="false">
										{{ app.user.name }}
									</button>
									<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
										<a class="dropdown-item" href="{{ path('profile') }}"><i class="material-icons">person_outline</i> Profile</a>
										<div class="divider"></div>
										{% if is_granted('ROLE_ADMIN') %}
										<a href="{{ path('admin') }}" class="dropdown-item"><i class="material-icons">dashboard</i> Dashboard</a>
										<div class="divider"></div>
										<a class="dropdown-item" href="{{ path('post_new') }}"><i class="material-icons">add</i> Add post</a>
										<a class="dropdown-item" href="{{ path('category_add') }}"><i class="material-icons">add</i> Add category</a>
										<div class="divider"></div>
										{% endif %}
										<a class="dropdown-item" href="{{ path('app_logout') }}"><i class="material-icons">power_settings_new</i> Logout</a>
									</div>
								</div>
							</li>
							{% else %}
							<li class="nav-item form-inline">
								<a href="{{ path('app_login') }}" class="nav-link">Login</a>
								or
								<a href="{{ path('register') }}" class="nav-link">Register</a>
							</li>
							{% endif %}

						</ul>
					</div>
				</div>
			</nav>